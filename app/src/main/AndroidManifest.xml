<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.demoproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Alarm.SetTimeActivity"></activity>
        <activity android:name=".AboutYourMedicine.YouMedActivity" />
        <activity android:name=".AboutYourMedicine.EditActivity" />
        <activity android:name=".AboutYourMedicine.AddNewActivity" />
        <activity android:name=".AllMedicine.ViewAllMedActivity" />
        <activity android:name=".AllMedicine.AllMedActivity" />
        <activity android:name=".User.RegisterActivity" />
        <activity android:name=".User.LoginActivity" />
        <activity android:name=".User.HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>